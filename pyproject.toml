[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "openrouter-cli"
version = "1.3.0"
description = "Terminal AI assistant: OpenRouter client + REPL + llm-router + tools"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }

[project.optional-dependencies]
cli = ["rich>=13"]
router = ["llm-router>=0.1", "anthropic>=0.60", "openai>=1.60"]

[project.scripts]
openrouter = "openrouter_cli.cli:main"

[tool.setuptools]
packages = ["openrouter_cli"]
